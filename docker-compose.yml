services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5179:5179"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
