repos:
  - repo: local
    hooks:
      - id: frontier-flow-verify
        name: frontier-flow verify
        entry: bun run verify
        language: system
        pass_filenames: false
        stages: [pre-commit]