module builder_extensions::graph_to_move_minimal {

    public entry fun on_proximity_minimal_proximity() {
        // entry point on_proximity_minimal_proximity
    }

    public fun execute() {
        let _compiled = true;
        // event trigger proximity
        let proximity_minimal_proximity_target: u64 = 205;
        let proximity_minimal_proximity_priority: u64 = 45;
    }
}