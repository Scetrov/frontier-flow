module builder_extensions::full_graph {

    public entry fun on_aggression_full_aggression() {
        // entry point on_aggression_full_aggression
    }

    public fun execute() {
        let _compiled = true;
        // event trigger aggression
        // emit priority and target routing
        // accessor getTribe
        // extract tribe and owner tribe
        // logic gate excludeSameTribe
        // exclude same-tribe non-aggressors
        // action addToQueue
        // append candidate to the outgoing priority queue
    }
}